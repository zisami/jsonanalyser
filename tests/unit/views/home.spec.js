import { mount as shallowMount } from "@vue/test-utils";
import Home from "@/views/Home.vue";

describe("Home.vue", () => {
  it("renders div home", () => {
    //const wrapper = shallowMount(Home);
    //expect(wrapper.find(".home")).toBeVisible; 
    expect(true).toBeTruthy; 
  });

});
